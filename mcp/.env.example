# Configuración de Hosts para comms_spymovil MCP
# Si los servicios corren localmente, usa localhost. 
# Si corren en Docker, usa los nombres de los servicios (ej. apiredis, apiconf).

APIREDIS_HOST=localhost
APIREDIS_PORT=5100

APICONF_HOST=localhost
APICONF_PORT=5200

APIDATOS_HOST=localhost
APIDATOS_PORT=5300

APICOMMS_HOST=localhost
APICOMMS_PORT=5000

MCP_DATABASE_URL="postgresql://mcp_user:pexco599@[IP_ADDRESS]/bd_spcomms"